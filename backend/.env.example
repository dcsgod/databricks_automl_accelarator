# Demo mode runs the full product against a built-in simulator. Set to false
# to drive a real Databricks workspace.
DEMO_MODE=true

# --- Real mode -------------------------------------------------------------
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_WAREHOUSE_ID=
DATABRICKS_CLUSTER_ID=
MLFLOW_EXPERIMENT=/Shared/agentic-automl
CHAMPION_MODEL_NAME=prod.ml_models.ts_forecast_champion

CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
